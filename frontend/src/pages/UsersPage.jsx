import { Box, Typography } from '@mui/material';

const UsersPage = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Usuários
      </Typography>
      <Typography>
        Gerenciamento de usuários (apenas admin) em desenvolvimento...
      </Typography>
    </Box>
  );
};

export default UsersPage;